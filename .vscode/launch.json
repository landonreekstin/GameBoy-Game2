{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Run (No Debug)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Clean_Up_Crew.gb",
            "preLaunchTask": "Run in mGBA"
        },
        {
            "name": "Debug with mGBA",
            "type": "cppdbg",
            "request": "attach",
            "miDebuggerServerAddress": "localhost:2345",
            "program": "${workspaceFolder}/build/Clean_Up_Crew.gb",
            "MIMode": "gdb",
            "preLaunchTask": "Run mGBA (GDB Server)"
        }
    ]
}